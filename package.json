{
    "scripts": {
        "build": "pug src/**/*.pug public --out public && sass src/**/*.scss public/css --no-source-map && del src/**/*.pug src/**/*.scss"
    },
    "dependencies": {
        "pug-cli": "^1.0.0-alpha6"
    }
}
