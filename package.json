{
    "devDependencies": {
      "pug-cli": "^1.0.0"
    },
    "scripts": {
      "build": "npm run obfuscate && npm run compile-pug",
      "compile-pug": "find . -name '*.pug' -exec pug --client --pretty {} --out $(dirname {}) \\; && find . -name '*.pug' -exec rm {} \\;"
    }
  }
  